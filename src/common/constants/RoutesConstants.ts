// Entity names and their base routes
export const ENTITY_NAME = Object.freeze({
	ENTITY: '/v1/test',
});

// Health check paths routes
export const HEALTH_CHECKS = Object.freeze({
	LIVE: '/v1/live',
	READY: '/v1/ready',
	HEALTH: '/v1/health',
});

export const ENTITY_ROUTES = Object.freeze({
	INDEX: '/',
});
